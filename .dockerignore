venv/
.venv/
__pycache__/
*.pyc
.env
.git/
.gitignore
.pytest_cache/
.vscode/
.claude/
outputs/
docs/
runs/
datasets/
tests/
*.mp4
*.avi
frame.jpg
# models/ SENGAJA tidak diabaikan — weights di-bake ke image supaya deploy ke
# HuggingFace Spaces / Railway (yang tanpa volume mount) bisa jalan sendiri.
# Yang dibuang cuma checkpoint mentah yang tidak dipakai runtime.
models/yolov8n.pt
models/last.pt
# Database absensi berisi vektor biometrik karyawan — JANGAN pernah ikut
# ter-bake ke dalam image. Di-mount sebagai volume lewat docker-compose.
data/
*.db
# Bobot wajah pihak ketiga (41 MB) diunduh saat runtime oleh
# src/fatigue/assets.py, dengan verifikasi sha256.
models/fatigue/*.onnx
models/fatigue/*.task
